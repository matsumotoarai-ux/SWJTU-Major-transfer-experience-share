# 报数游戏（指针访问一维数组）

## 题目

`n` 个人围成一圈，按顺序编号为 `1` 到 `n`。从编号 `1` 的人开始循环报数，每次从 `1` 报到 `4`；报到 `4` 的人退出。求最后留下的人的编号。

## 思路

创建一维数组保存每个人的编号，用指针逐个访问。退出的人所在位置改为 `0`，之后报数时跳过该位置。指针走到数组末尾后回到开头，直到只剩一人。

## C 语言代码

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int n;
    if (scanf("%d", &n) != 1 || n <= 0) {
        return 0;
    }

    int *people = malloc((size_t)n * sizeof *people);
    if (people == NULL) {
        return 1;
    }

    for (int *p = people; p < people + n; ++p) {
        *p = (int)(p - people) + 1;
    }

    int remaining = n;
    int count = 1;
    int *p = people;

    while (remaining > 1) {
        if (*p != 0) {
            if (count == 4) {
                *p = 0;
                --remaining;
                count = 1;
            } else {
                ++count;
            }
        }

        ++p;
        if (p == people + n) {
            p = people;
        }
    }

    for (p = people; p < people + n; ++p) {
        if (*p != 0) {
            printf("最后留下的人编号为：%d\n", *p);
            break;
        }
    }

    free(people);
    return 0;
}
```

## 样例

输入：

```text
10
```

输出：

```text
最后留下的人编号为：5
```

代码通过 `*p` 访问数组元素，符合“用指针变量访问一维数组”的要求。

---

# T2：三位数的“重排求差”

## 题目

输入一个各位数字不全相同的三位自然数。每次将它的三个数字分别排成最大数和最小数，再用最大数减去最小数；重复这一操作，输出直到得到 `495` 为止的每一步。

例如，`763` 的计算过程为 `763-367=396`、`963-369=594`、`954-459=495`。

## 函数设计

```text
main
├── Input：读取并返回输入值
└── Check：重复计算并输出每一步
    ├── Max：将三个数字按从大到小排列
    └── Min：将 Max 的三个数字逆序排列
```

计算过程中可能出现前导零，例如 `100-001=099`。输出时使用 `%03d` 保持三位格式。

## C 语言代码

```c
#include <stdio.h>

int Input(void);
void Check(int n);
int Max(int n);
int Min(int n);

int Input(void)
{
    int n;
    if (scanf("%d", &n) != 1) {
        return 0;
    }
    return n;
}

int Max(int n)
{
    int a = n / 100;
    int b = (n / 10) % 10;
    int c = n % 10;
    int temp;

    if (a < b) {
        temp = a;
        a = b;
        b = temp;
    }
    if (a < c) {
        temp = a;
        a = c;
        c = temp;
    }
    if (b < c) {
        temp = b;
        b = c;
        c = temp;
    }

    return a * 100 + b * 10 + c;
}

int Min(int n)
{
    int a = n / 100;
    int b = (n / 10) % 10;
    int c = n % 10;
    return c * 100 + b * 10 + a;
}

void Check(int n)
{
    int largest, smallest, difference;

    do {
        largest = Max(n);
        smallest = Min(largest);
        difference = largest - smallest;
        printf("%03d-%03d=%03d\n", largest, smallest, difference);
        n = difference;
    } while (n != 495);
}

int main(void)
{
    int n = Input();
    int a = n / 100;
    int b = (n / 10) % 10;
    int c = n % 10;

    if (n < 100 || n > 999 || (a == b && b == c)) {
        return 0;
    }

    Check(n);
    return 0;
}
```

## 样例

输入：

```text
789
```

输出：

```text
987-789=198
981-189=792
972-279=693
963-369=594
954-459=495
```
